import type { FunctionComponent } from "../common/types";
import { BookFullCard } from "../components/layout/BookFullCard";

const Book = (): FunctionComponent => {
	return (
		<main>
      <BookFullCard />
    </main>
	);
};

export default Book;
